require('dotenv').config(); const jwt = require('jsonwebtoken'); const testId = 'test-user-id'; const token = jwt.sign({ id: testId }, process.env.JWT_SECRET, { expiresIn: process.env.JWT_EXPIRE }); console.log('Generated Token:', token); console.log('JWT_EXPIRE:', process.env.JWT_EXPIRE); const decoded = jwt.verify(token, process.env.JWT_SECRET); console.log('Decoded Token:', decoded);
